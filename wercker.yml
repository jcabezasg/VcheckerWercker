# Specify the main box to be used
box:
  id: jcabezasg/vcheckerwercker
  tag: latest

build:

 steps:
    - script:
       name: Show current working folder 
       code: echo $PWD
    - script:
       name: Show current working folder 
       code: ls -la

